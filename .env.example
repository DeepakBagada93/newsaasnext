# Supabase Project Credentials
# Find these in your Supabase project's "Settings" > "API" section.
NEXT_PUBLIC_SUPABASE_URL="your-supabase-url"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"
SERVICE_ROLE_KEY="your-supabase-service-role-key"

# Gmail SMTP Credentials for Nodemailer (for contact forms)
# IMPORTANT: You must use an "App Password" for GMAIL_APP_PASSWORD, not your regular Gmail password.
# 1. Enable 2-Step Verification on your Google Account.
# 2. Go to https://myaccount.google.com/apppasswords to generate a new 16-character App Password.
GMAIL_USER="your-gmail-address@gmail.com"
GMAIL_APP_PASSWORD="your-16-character-app-password"

# Recipient email address for messages sent via the contact and recommendation forms.
CONTACT_FORM_RECIPIENT_EMAIL="your-recipient-email@example.com"
